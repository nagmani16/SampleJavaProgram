package jaiMataDiSaaiBaba;

public class SelectionSort {
	
	/*
	 * the logic of selection sort is to get the next minimum number and swap it to "i" th position
	 * "i" th position can driven from outer loop
	 * set initially the 0th position will be the minimum (imin)
	 * run the second loop from j position which is (i+1) to length of the array
	 * check if the element at (i+1) position is lesser than imin, if yes then make that as imin index and move forward
	 * once we have the imin index then swap it with "i" th position
	 * increment i and reapeat 
	 */

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
